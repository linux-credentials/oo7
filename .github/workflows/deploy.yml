name: Deploy to GitHub Pages

on:
  workflow_run:
    workflows: ["docs", "Code Coverage"]
    types:
      - completed
    branches:
      - main

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages

jobs:
  deploy:
    name: Deploy Combined Site
    runs-on: ubuntu-latest
    if: github.event.workflow_run.conclusion == 'success'

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Setup Pages
        uses: actions/configure-pages@v6

      - name: Find latest successful workflow runs
        id: find-runs
        uses: actions/github-script@v9
        with:
          script: |
            // Find latest successful docs run
            const { data: runs } = await github.rest.actions.listWorkflowRuns({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'docs.yml',
              status: 'completed',
              conclusion: 'success',
              branch: 'main',
              per_page: 1
            });
            const docsRun = runs.workflow_runs[0];

            // Find latest successful coverage run
            const { data: coverageRuns } = await github.rest.actions.listWorkflowRuns({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'coverage.yml',
              status: 'completed',
              conclusion: 'success',
              branch: 'main',
              per_page: 1
            });
            const coverageRun = coverageRuns.workflow_runs[0];

            if (!docsRun || !coverageRun) {
              console.log('Waiting for both workflows to complete, skipping deploy for now');
              core.setOutput('ready', 'false');
              return;
            }

            core.setOutput('ready', 'true');
            core.setOutput('docs-run-id', docsRun.id);
            core.setOutput('coverage-run-id', coverageRun.id);
            console.log(`Found docs run: ${docsRun.id}, coverage run: ${coverageRun.id}`);

      - name: Download docs artifact
        if: steps.find-runs.outputs.ready == 'true'
        uses: actions/download-artifact@v8
        with:
          name: docs-reports
          path: ./site/
          github-token: ${{ secrets.GITHUB_TOKEN }}
          run-id: ${{ steps.find-runs.outputs.docs-run-id }}

      - name: Download coverage artifact
        if: steps.find-runs.outputs.ready == 'true'
        uses: actions/download-artifact@v8
        with:
          name: coverage-reports
          path: ./site/coverage/
          github-token: ${{ secrets.GITHUB_TOKEN }}
          run-id: ${{ steps.find-runs.outputs.coverage-run-id }}

      - name: Upload combined site
        if: steps.find-runs.outputs.ready == 'true'
        uses: actions/upload-pages-artifact@v5
        with:
          path: ./site/

      - name: Deploy to GitHub Pages
        if: steps.find-runs.outputs.ready == 'true'
        id: deployment
        uses: actions/deploy-pages@v5
